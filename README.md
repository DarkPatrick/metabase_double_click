# metabase_double_click
Tampermonkey script making double click on small circle buttons to select only one legend (and double click on a single active button again to turn on all legends back)

set you metabase domain in @match and you are ready to go
